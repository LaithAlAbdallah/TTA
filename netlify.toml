[build]
  publish = "dist/demo/browser"
  command = "npm run build:prod"

# SPA routing: serve index.html for all paths so Angular handles /, /about, /services
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
